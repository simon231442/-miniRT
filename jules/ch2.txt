The Ray Tracer Challenge – Chapitre 2
Fiche de révision : Rayons, Sphères et Intersections

🎯 Objectif
Ce chapitre introduit les concepts de base du ray tracing :
- Définir un rayon
- Intersecter un rayon avec une sphère
- Identifier les points de contact (hits)

---

🧭 Le rayon (Ray)
Un rayon est défini par :
- Origine (point)
- Direction (vecteur unitaire)

Formule d’un point sur le rayon :
P(t) = origine + t * direction
- t ≥ 0 pour avancer le long du rayon
- t < 0 pour revenir en arrière

---

⚪ La sphère
- Définie par un centre (point) et un rayon
- Par défaut, centre = (0,0,0), rayon = 1
- Une sphère peut être transformée via des matrices (translation, scaling, rotation)

---

➕ Intersection rayon/sphère
Pour un rayon R(t) et une sphère S :
1. Calculer le vecteur L = origine_ray - centre_sphère
2. Résoudre l’équation quadratique :
   a = direction·direction
   b = 2 * direction·L
   c = L·L - rayon²
   Δ = b² - 4ac
3. Si Δ < 0 → pas d’intersection
   Si Δ = 0 → 1 intersection (tangent)
   Si Δ > 0 → 2 intersections

---

🎯 Le hit
- C’est la **plus petite valeur positive de t** correspondant à une intersection
- Permet de savoir **quelle intersection est visible** depuis l’œil

---

🔄 Résumé opérationnel
1. Créer le rayon : origine + direction
2. Calculer intersections avec la sphère
3. Identifier le hit (plus petit t > 0)
4. Calculer le point d’impact : P(hit)
5. Calculer la normale de la sphère au point d’impact :
   N = (P - centre) normalisé

---

✅ À retenir
- Les rayons sont des vecteurs paramétriques
- Les sphères ont une formule simple pour les intersections
- Le hit permet de choisir l’intersection visible
- La normalisation de la normale est essentielle pour l’éclairage
- Les transformations appliquées à la sphère modifient le calcul des intersections

---
Fiche rédigée d’après : The Ray Tracer Challenge de Jamis Buck

